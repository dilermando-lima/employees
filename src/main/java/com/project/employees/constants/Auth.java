package com.project.employees.constants;

public interface Auth {
    

    final public static String SECRET_TOKEN = "This is only to use in a simple example. don't do this on a real project";
    final public static String PUBLIC_COMMON_TOKEN = "This token is only for a simple test. don't do this on a real project";
    final public static String HEADER_AUTH_NAME = "Authorization";

}
